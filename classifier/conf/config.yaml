defaults:
  - dataset: flowers102
  - model: convnext_tiny
  - trainer: default
  - _self_
# Global settings
seed: 42
output_dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
# Hydra configuration
hydra:
  run:
    dir: .
  output_subdir: null
